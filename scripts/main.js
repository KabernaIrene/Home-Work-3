const name = window.prompt ("Your name");
const surname = window.prompt ("Your surname");
const age = window.prompt ("Your age", 0);

const identify = alert("You are" + " " + name + " " + surname + ", " + age + " " + "years");

const numbers = window.prompt ("Write number");

const number = alert (numbers .split("").join(" "));

